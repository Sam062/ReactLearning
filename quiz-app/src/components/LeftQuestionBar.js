import React from 'react'
import { useQuestionData } from './useQuestionData';
import { BrowserRouter as Router, Routes, Route, Link } from 'react-router-dom';

export const LeftQuestionBar = () => {
    const questions = useQuestionData();
    return (
        <></>
    )
}
